<%= simple_form_for @shark do |f| %>
  <%= f.input :name %>
  <%= f.input :age %>
  <%= f.input :breed %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :diet %>
  <%= f.input :picture_url %>
  <%= f.button :submit %>
<% end %>

<%# :name, :age, :type, :description, :price, :diet, :picture_url %>
